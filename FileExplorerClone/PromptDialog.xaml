<Window x:Class="FileExplorerClone.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FileExplorerClone"
        Title="Prompt" SizeToContent="Height" Width="440"
        FontSize="{StaticResource AppFontSize}"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" Icon="Resources/Dvojnik.ico">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptLabel" Grid.Row="0" Margin="0,0,0,8" TextWrapping="Wrap"/>
        <TextBox x:Name="InputBox" Grid.Row="1" KeyDown="InputBox_KeyDown"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button MinWidth="90" Padding="10,4" Margin="0,0,8,0" Click="OkButton_Click" IsDefault="True"
                    Content="{Binding [Dialog_OK], Source={x:Static local:Loc.Instance}}"/>
            <Button MinWidth="90" Padding="10,4" IsCancel="True"
                    Content="{Binding [Dialog_Cancel], Source={x:Static local:Loc.Instance}}"/>
        </StackPanel>
    </Grid>
</Window>
